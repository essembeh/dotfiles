if test -x /usr/bin/exa; then
	unset LS_COLORS
	alias ls="exa --color-scale --classify"
	alias l="ls --all"
	alias ll="ls --long"
	alias la="ll --all"
	alias llt="ll --sort=modified"
	alias lls="ll --sort=size"
	alias tree="ls --tree"
fi
