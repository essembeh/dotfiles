[user]
	name = Sébastien MB
	email = seb@essembeh.org
[core]
	editor = vim
	pager = less
[fetch]
	prune = true
[pull]
	rebase = preserve
[push]
	default = simple
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
[alias]
	b = branch -a -vv
	co = checkout
	s = status
	f = fetch --tags
	cp = cherry-pick
	history = log --follow
	ls = log --oneline --graph --decorate
	ll = log --pretty=format:%C(yellow)%h\\ %C(blue)%ai\\ %C(cyan)[%cn]\\ %Creset%s\\ %Cred%d --decorate 
	find = !git ls-files | grep -i
	undo = reset --soft HEAD^
	amend = commit --amend --date now
	commit-reusemessage = commit --reedit-message=HEAD --reset-author
	rebase-upstream = rebase -i @{u}
	spull = !git stash && git pull && git stash pop
	please = push --force-with-lease
[status]
	showUntrackedFiles = all
[url "git@github.com:"]
	pushInsteadOf = https://github.com/
