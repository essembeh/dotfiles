[user]
	name = Sébastien MB
	email = seb@essembeh.org
[core]
	editor = vim
	pager = less
[fetch]
	prune = true
[pull]
[push]
	default = simple
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
[alias]
	r = review -l -l
	b = branch -a -vv
	co = checkout
	s = status
	f = fetch --tags --all
	cp = cherry-pick
	find = !git ls-files | grep -i
	undo = reset --soft HEAD^
	amend = commit --amend --date now
	commit-reusemessage = commit --reedit-message=HEAD --reset-author
	iw = commit --all --message='DO NOT MERGE: in work'
	spull = !git stash && git pull && git stash pop
	please = push --force-with-lease
[status]
	showUntrackedFiles = all
[url "git@github.com:"]
	pushInsteadOf = https://github.com/
